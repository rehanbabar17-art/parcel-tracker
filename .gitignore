# Local config with real tracking numbers + ntfy topic - never commit
config.json

# Tracking state (statuses/locations) - persisted via GitHub Actions cache
state.json

# Node & Build artifacts
node_modules/
dist/
.env
